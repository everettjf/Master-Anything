# Calc App

## Overview

This app provides a small calculator. Read this before the usage section.

## Using the Calculator

The `Calculator` class is the entry point. Create one and call its methods.
Use `add_many` to sum a list of numbers in one call.

## Averages

The `average` helper builds on `Calculator` to compute the mean of a list,
returning 0 for an empty list.
